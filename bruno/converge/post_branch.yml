info:
  name: post_branch
  type: http
  seq: 13

http:
  method: POST
  url: http://localhost:8080/api/branches
  body:
    type: json
    data: |-
      {
          "name": "Siam",
          "capacity": 30
        }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
