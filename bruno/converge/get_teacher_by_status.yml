info:
  name: get_teacher_by_status
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/teachers?status=active
  params:
    - name: status
      value: "active"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
