info:
  name: delete_parent_student
  type: http
  seq: 24

http:
  method: DELETE
  url: http://localhost:8080/api/parents/1/students/3
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
