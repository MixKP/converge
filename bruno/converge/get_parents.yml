info:
  name: get_parents
  type: http
  seq: 21

http:
  method: GET
  url: http://localhost:8080/api/parents
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
