info:
  name: delete_booking
  type: http
  seq: 32

http:
  method: DELETE
  url: http://localhost:8080/api/bookings/1
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
