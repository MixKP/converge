info:
  name: get_students
  type: http
  seq: 20

http:
  method: GET
  url: http://localhost:8080/api/students
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
