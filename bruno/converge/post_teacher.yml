info:
  name: post_teacher
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8080/api/teachers
  body:
    type: json
    data: |-
      {
          "name": "Alice",
          "email": "alice@example.com",
          "gender": "female"
        }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
