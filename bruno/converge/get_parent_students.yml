info:
  name: get_parent_students
  type: http
  seq: 22

http:
  method: GET
  url: http://localhost:8080/api/parents/1/students
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
