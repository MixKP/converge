info:
  name: get_commute
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:8080/api/commute
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
